BMSApp.ns('BMSApp.pages');
BMSApp.pages.BorrowListController = function () {
    //this.bookListFunc = Template7.compile($$("#BookListTemplate").html());
};
BMSApp.pages.BorrowListController.prototype.preprocess = function (content, url, next) {
    next(content);
};

BMSApp.pages.BorrowListController.prototype.pageInit = function (page) {
    var self = this;

    
};
